# This file is auto-generated. Do not edit manually.
# Source: .github/ansible-setup-targets.yml

name: Setup Editor

on:
  workflow_call:

permissions:
  contents: read

jobs:
  setup-editor:
    runs-on: macos-15
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          persist-credentials: false

      - name: Download mev binary
        uses: ./.github/actions/download-mev-binary

      - name: Setup Ansible environment
        uses: ./.github/actions/setup-ansible

      - name: Run editor setup
        shell: bash
        run: |
          set -euo pipefail
          "$RUNNER_TEMP/mev-bin/mev" make vscode
          "$RUNNER_TEMP/mev-bin/mev" make antigravity-ide
          "$RUNNER_TEMP/mev-bin/mev" make zed
          "$RUNNER_TEMP/mev-bin/mev" make xcode
